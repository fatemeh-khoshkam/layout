import styled from "styled-components";
import { Link } from "react-router-dom";

const LogoImg = styled.img`
  align-self: start;
  width: 100px;
`;

const StyledLink = styled(Link)`
  text-decoration: none;
`;

export { LogoImg, StyledLink };
